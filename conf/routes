# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /subcount/register                   controllers.Application.register

GET     /subcount/registered                 controllers.Application.registered(code: String, scope: String, state: String)

GET     /subcount/:channel          controllers.Application.subcount(channel: String)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

    